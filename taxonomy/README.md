# Taxonomy

The taxonomy names recurring review-bug classes.

Start with [`review-bug-classes.md`](review-bug-classes.md). Classify by the
smallest enforcing layer that should prevent recurrence, not by the file where
the reviewer left the comment.
